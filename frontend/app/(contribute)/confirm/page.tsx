import { PagePlaceholder } from "@/components/page-placeholder";

export default function ConfirmPage() {
  return (
    <PagePlaceholder
      route="/confirm"
      title="Confirm"
      description="One item, one question, three buttons, nothing else on the page."
    />
  );
}
