NODE_ENV=development
DATABASE_URL="postgresql://user:password@localhost:6532/opendarja?pgbouncer=true"
DIRECT_URL="postgresql://user:password@localhost:5532/opendarja"
AUTH_SECRET="generate with: openssl rand -base64 32"
CORS_ORIGIN="http://localhost:3001"
PORT=3000

IP_HASH_SALT="generate with: openssl rand -base64 32"

REDIS_URL="redis://localhost:6379"

# local | r2
STORAGE_PROVIDER=local
STORAGE_LOCAL_DIR="./.storage"
STORAGE_LOCAL_BASE_URL="http://localhost:3000"
STORAGE_LOCAL_SECRET="generate with: openssl rand -base64 32"
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=
