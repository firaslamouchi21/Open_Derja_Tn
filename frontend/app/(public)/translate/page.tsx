import { PagePlaceholder } from "@/components/page-placeholder";

export default function TranslatePage() {
  return (
    <PagePlaceholder
      route="/translate"
      title="Translate"
      description="Live translator, both directions, region-aware, honest about what it hasn't got."
    />
  );
}
