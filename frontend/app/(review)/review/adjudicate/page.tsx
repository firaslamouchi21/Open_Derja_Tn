import { PagePlaceholder } from "@/components/page-placeholder";

export default function AdjudicationPage() {
  return (
    <PagePlaceholder
      route="/review/adjudicate"
      title="Adjudication"
      description="Conflicting annotations side by side with who made them; pick or override. Region-routed."
    />
  );
}
