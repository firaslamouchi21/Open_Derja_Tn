[package]
name = "opendarja-processing"
version = "0.0.0"
edition = "2021"

[[bin]]
name = "opendarja-processing"
path = "src/main.rs"

[dependencies]
actix-web = { version = "4", default-features = false, features = ["macros"] }
serde = { version = "1", features = ["derive"] }
