import { PagePlaceholder } from "@/components/page-placeholder";

export default function LexiconPage() {
  return (
    <PagePlaceholder
      route="/lexicon"
      title="Lexicon"
      description="Concept list, regional variants, attestation counts."
    />
  );
}
