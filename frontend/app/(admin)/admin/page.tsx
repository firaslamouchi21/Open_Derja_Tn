import { PagePlaceholder } from "@/components/page-placeholder";

export default function AdminDashboardPage() {
  return (
    <PagePlaceholder
      route="/admin"
      title="Admin dashboard"
      description="Overview widgets plus data management, review lanes, task admin, sources, users, snapshots, privacy operations, system."
    />
  );
}
