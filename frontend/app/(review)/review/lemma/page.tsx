import { PagePlaceholder } from "@/components/page-placeholder";

export default function LemmaLinkingPage() {
  return (
    <PagePlaceholder
      route="/review/lemma"
      title="Lemma linking"
      description="Attach tokens to lexicon variants. Core annotation work — the translator runs on it."
    />
  );
}
