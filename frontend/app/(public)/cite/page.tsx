import { PagePlaceholder } from "@/components/page-placeholder";

export default function CitationPage() {
  return (
    <PagePlaceholder
      route="/cite"
      title="Citation"
      description="BibTeX, Zenodo DOI, version-specific citation strings."
    />
  );
}
