import { PagePlaceholder } from "@/components/page-placeholder";

export default function MyContributionsPage() {
  return (
    <PagePlaceholder
      route="/me"
      title="My contributions"
      description="What was contributed and whether it was accepted. Not a level, not a tier, not a path."
    />
  );
}
