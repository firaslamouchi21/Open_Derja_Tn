import { PagePlaceholder } from "@/components/page-placeholder";

export default function CorrectionsPage() {
  return (
    <PagePlaceholder
      route="/review/corrections"
      title="Corrections"
      description="Community fixes, origin proposals, publication submissions, flags."
    />
  );
}
