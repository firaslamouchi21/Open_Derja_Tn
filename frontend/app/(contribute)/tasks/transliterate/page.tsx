import { PagePlaceholder } from "@/components/page-placeholder";

export default function TransliterateTaskPage() {
  return (
    <PagePlaceholder
      route="/tasks/transliterate"
      title="Transliterate task"
      description="Segment plus rule-generated suggestion, uncertain spans highlighted."
    />
  );
}
