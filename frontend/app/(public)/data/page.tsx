import { PagePlaceholder } from "@/components/page-placeholder";

export default function DownloadPage() {
  return (
    <PagePlaceholder
      route="/data"
      title="Download"
      description="Versioned releases, formats, checksums, licence per tier, changelog."
    />
  );
}
