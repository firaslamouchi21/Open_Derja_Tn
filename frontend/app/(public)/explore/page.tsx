import { PagePlaceholder } from "@/components/page-placeholder";

export default function ExplorePage() {
  return (
    <PagePlaceholder
      route="/explore"
      title="Explore"
      description="Search and filters: region, scope, era, setting, register, level, script, has-translation."
    />
  );
}
