import { PagePlaceholder } from "@/components/page-placeholder";

export default function SupportPage() {
  return (
    <PagePlaceholder
      route="/support"
      title="Support"
      description="Donations and sponsorship only — nothing sold, nothing gated behind it."
    />
  );
}
