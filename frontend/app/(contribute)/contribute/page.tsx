import { PagePlaceholder } from "@/components/page-placeholder";

export default function ContributePage() {
  return (
    <PagePlaceholder
      route="/contribute"
      title="Contribute"
      description="One box: write a word, sentence, or paragraph. Region as four tappable buttons, consent as one checkbox, no account."
    />
  );
}
